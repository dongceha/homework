import React from 'react';

const NotFound = ({location}) => {
    console.log(location);
    return (
        <div>
        404
            {/* {location} */}
        </div>
    );
}

export default NotFound;
